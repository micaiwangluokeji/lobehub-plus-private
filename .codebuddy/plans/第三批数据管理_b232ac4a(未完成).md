---
name: 第三批数据管理
overview: 开发管理后台第三批4个数据管理模块：消息管理、文件管理、知识库管理、API Key管理。完成列表+搜索+CRUD+删除全流程，激活侧边栏导航，补充i18n键。
todos:
  - id: verify-current-state
    content: 核实第三批4个模块代码完整性（已完成，无缺失文件）
    status: pending
  - id: create-i18n-json
    content: 创建 locales/en-US/admin.json 和 locales/zh-CN/admin.json
    status: pending
    dependencies:
      - verify-current-state
  - id: update-plan
    content: 更新 plan.md，确认第三批完成，将第五批设为下个执行目标
    status: pending
    dependencies:
      - create-i18n-json
---

## 第三批数据管理开发状态核实

经过代码探查，消息管理、文件管理、知识库管理、API Key 管理这4个模块的**所有文件已全部存在**：

### 已完成文件清单

| 文件类别 | 状态 | 说明 |
| --- | --- | --- |
| **Service 层** (4个) | 已全部存在 | `messages.ts`, `files.ts`, `knowledge-bases.ts`, `api-keys.ts` — 含完整类型定义和CRUD方法 |
| **UI 组件** (8个) | 已全部存在 | `MessageList.tsx`(8.7KB), `FileList.tsx`(5.5KB), `KnowledgeBaseList.tsx`(8.0KB), `ApiKeyList.tsx`(7.0KB) + 对应index.ts |
| **路由文件** (4个) | 已全部存在 | `messages/index.tsx`, `files/index.tsx`, `knowledge-bases/index.tsx`, `api-keys/index.tsx` |
| **路由配置** (2个config) | 已全部存在 | `desktopRouter.config.tsx` + `.desktop.tsx` 均已配置路由 |
| **侧边栏** | 已配置 | 4个路径均在 `isComingSoon` 白名单中，可点击导航 |
| **i18n 源文件** | 已存在 | `src/locales/default/admin.ts` 含所有4个模块的翻译键（共312行） |
| **i18n JSON文件** | 缺失 | `locales/en-US/admin.json` 和 `locales/zh-CN/admin.json` 不存在，需手工创建 |


### 结论

第三批数据管理模块的开发代码已全部完成，仅需补充 i18n JSON 文件。当前真正待开发的是 PRD 中新增的**第五批商业管理模块**（支付配置 + 套餐/积分管理），但这2个模块需等后端补充 endpoints 后才能开发前端。

## 技术方案

### 当前剩余工作

1. **补充 i18n JSON 文件**（立即执行）：

- 创建 `locales/en-US/admin.json` — 将 `src/locales/default/admin.ts` 的英文翻译镜象过去
- 创建 `locales/zh-CN/admin.json` — 手写中文翻译
- 这两个JSON文件是CI自动翻译流程的输入源，也是UI展示的必要文件

2. **第五批商业管理**（需确认后端进度后执行）：

- 支付配置 `/admin/payment` — 需后端 `paymentConfigEndpoints`
- 套餐/积分管理 `/admin/plans` — 需后端 `planRouter` + `creditConfigEndpoints`
- 当前这些后端 endpoints 为空壳，需等后端团队补充后再开发