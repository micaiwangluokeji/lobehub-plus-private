# 2026-07-07

## GitHub ↔ Gitee canary 自动同步

- 创建了 `.github/workflows/sync-canary-to-gitee.yml`，触发方式：push 到 canary、每4小时定时、手动触发
- 当 GitHub 点 "Sync fork" 同步 canary，或定时检查到上游更新时，自动 force push 到 Gitee 的 canary 分支
- 文件已推送到 GitHub `piepiepie/lobehub-plus` 的 `canary` 分支（commit `8df106d` → `1233e603`）
- **GITEE_TOKEN Secret 已配置完成**
- **⚠️ 首次运行失败**：URL 内嵌 Token 方式（`https://pieth:TOKEN@gitee.com/...`）在 GitHub Actions runner 上返回 exit code 128（Authentication failed），但本地测试完全正常
- **修复方案**：改用 `git config --global credential.helper store` + 写入 `~/.git-credentials` 方式推送，成功通过（commit `1233e603`）
- **经验教训**：GitHub Actions 的 git runner 对 URL 内嵌凭证的处理可能与本地不同，credential store 方式更可靠

## localhost:4010 页面检查

- 用户要求打开 http://localhost:4010/ 查看报错
- 页面 302 重定向到登录页 `/signin`（未登录状态正常行为）
- 检查结果：无 Console 错误、无页面 JS 错误、无网络请求失败
- 页面正常渲染，Title 为 "Sign In"

## 修复 SuggestionChips TypeError（swrKeys 缺少 agentBuilder）

- WebView 报错 `TypeError: Cannot read properties of undefined (reading 'suggestions')` 在 `useBuilderSuggestions.ts:17:84`
- 根因：`useBuilderSuggestions.ts` 第 65 行调用 `swrKeys.agentBuilder.suggestions(...)`，但 `agentBuilder` 命名空间从未在 `src/libs/swr/keys.ts` 中定义
- 修复：在 keys.ts 中添加 `agentBuilderKeys`（含 `suggestions` key factory），并注册到 `swrKeys` 聚合对象
- 0 lint 错误
