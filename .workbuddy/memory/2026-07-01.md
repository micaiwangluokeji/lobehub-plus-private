# 2026-07-01 工作日志

## LobeHub RBAC 与工作区管理页面调研

- 调研了 LobeHub 的 RBAC 权限体系和工作区管理前端页面结构
- 工作区设置页面路由：`/:workspaceSlug/settings/*`，路由文件位于 `src/routes/(main)/[workspaceSlug]/settings/`
- 侧边栏定义：`src/features/WorkspaceSetting/hooks/useCategory.tsx`，按 4 个分组（General/Subscription/Agent/Admin）组织
- Admin 分组（Storage + API Key）仅 Owner 可见，判断条件：`useIsWorkspaceOwner()`（当前硬编码返回 true）
- 社区工作区设置页面：`src/features/CommunityWorkspaceSettings/index.tsx`，路由 `/community/workspace/settings`
- RBAC 权限常量定义：`packages/const/src/rbac.ts`，包含 80+ 权限代码和 3 个工作区内置角色（owner/member/viewer）
- 全局超级管理员 `super_admin` 在代码中仅有角色定义，无前端管理面板
- 路由注册：`src/spa/router/desktopRouter.config.desktop.tsx`
